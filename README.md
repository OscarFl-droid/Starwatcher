# StarWatcher v4.5 — zenith compass view

Re-zero is now true zenith (90° elevation, directly overhead). Focused 15°, 30°, 60° and 90° modes are cones around the zenith. With compass alignment enabled, turning the phone rotates the sky map so the top of the map matches the phone bearing. Added more bright reference stars and labels.

Upload as an overlay. Keep data/catalogue.json unchanged.
